# 🦁☀️ راهنمای سریع نصب و راه‌اندازی

## ربات تلگرام انقلاب ملی ۱۴۰۴

---

## 📥 مرحله ۱: نصب نرم‌افزارهای مورد نیاز

### Windows

1. **Python 3.8 یا بالاتر**
   - دانلود از: https://www.python.org/downloads/
   - حتماً گزینه "Add Python to PATH" را تیک بزنید

2. **ffmpeg** (برای حذف متادیتا)
   - دانلود از: https://ffmpeg.org/download.html
   - یا با Chocolatey:
   ```powershell
   choco install ffmpeg
   ```

---

## 🤖 مرحله ۲: ساخت ربات تلگرام

1. تلگرام را باز کنید
2. به [@BotFather](https://t.me/BotFather) مراجعه کنید
3. دستور `/newbot` را بفرستید
4. یک نام برای ربات انتخاب کنید (مثلاً: Revolution1404Bot)
5. یک username برای ربات انتخاب کنید (باید به bot ختم شود)
6. **توکن ربات را کپی کنید** - به این شکل است:
   ```
   1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
   ```

---

## ⚙️ مرحله ۳: پیکربندی ربات

1. فایل `config.py` را باز کنید
2. توکن ربات را جایگزین کنید:
   ```python
   BOT_TOKEN = "1234567890:ABCdefGHIjklMNOpqrsTUVwxyz"  # توکن خود را اینجا بگذارید
   ```

3. آدرس Mini App را تنظیم کنید (مرحله ۵)

---

## 📦 مرحله ۴: نصب کتابخانه‌های Python

در پوشه پروژه، این دستور را اجرا کنید:

```powershell
pip install -r requirements.txt
```

منتظر بمانید تا تمام کتابخانه‌ها نصب شوند (چند دقیقه طول می‌کشد).

---

## 🌐 مرحله ۵: نصب Mini App (برنامه ایمیل)

برنامه وب `webapp/index.html` باید روی یک سرور HTTPS قرار گیرد.

### گزینه ۱: GitHub Pages (رایگان - پیشنهادی)

1. یک مخزن GitHub بسازید
2. فایل `webapp/index.html` را آپلود کنید
3. در Settings > Pages فعال کنید
4. آدرس شما چیزی شبیه این خواهد بود:
   ```
   https://yourusername.github.io/reponame/webapp/index.html
   ```

### گزینه ۲: Netlify (رایگان)

1. به https://netlify.com مراجعه کنید
2. پوشه `webapp` را آپلود کنید
3. آدرس HTTPS دریافت کنید

### گزینه ۳: موقت با ngrok

```powershell
cd webapp
python -m http.server 8000
```

در ترمینال دیگری:
```powershell
ngrok http 8000
```

آدرس HTTPS را کپی کنید.

**مهم:** آدرس را در `config.py` بگذارید:
```python
WEBAPP_URL = "https://your-actual-url.com/webapp/index.html"
```

---

## ✅ مرحله ۶: بررسی نصب

دستور زیر را اجرا کنید:

```powershell
python setup_check.py
```

باید همه چیز ✅ سبز باشد.

---

## 🚀 مرحله ۷: اجرای ربات

```powershell
python bot.py
```

باید این پیام را ببینید:
```
INFO - Bot started successfully! 🦁☀️
```

---

## 📱 مرحله ۸: تست ربات

1. تلگرام را باز کنید
2. ربات خود را پیدا کنید (با username که ساختید)
3. دستور `/start` را بفرستید
4. باید پیام خوشامدگویی به فارسی ببینید
5. دکمه‌های فارسی ظاهر می‌شوند

---

## 🎯 عملیات‌های اصلی

### ✅ آزمایش ارسال ایمیل
1. دکمه "ارسال ایمیل هدفمند 📧" را بزنید
2. یک برنامه وب باز می‌شود
3. روی یکی از دکمه‌ها کلیک کنید
4. ایمیل شما باز می‌شود با متن آماده

### ✅ آزمایش توییت
1. دکمه "توییت عملیاتی  🐦" را بزنید
2. یک توییت تصادفی تولید می‌شود
3. دکمه "توییت کن" را بزنید
4. توییتر باز می‌شود

### ✅ آزمایش ارسال ویدیو (امنیت)
1. دکمه "ارسال مستندات جنایات 📹" را بزنید
2. یک ویدیو ارسال کنید
3. ربات متادیتا را حذف می‌کند
4. ویدیوی پاک شده را برمی‌گرداند

---

## 🐛 مشکلات رایج

### ربات استارت نمی‌شود
```
ERROR: Token is invalid
```
**راه‌حل:** توکن را در `config.py` چک کنید

### ffmpeg پیدا نمی‌شود
```
ffmpeg not found
```
**راه‌حل:** ffmpeg را نصب کنید و در PATH بگذارید

### Mini App باز نمی‌شود
**راه‌حل:** 
- آدرس باید HTTPS باشد
- فایل HTML باید روی اینترنت باشد
- آدرس را در مرورگر تست کنید

---

## 📊 سیستم امتیازدهی

| عملیات | امتیاز |
|--------|--------|
| لاگین روزانه | 2 |
| ارسال ایمیل | 10 |
| توییت | 5 |
| ارسال ویدیو | 15 |
| Conduit تأیید شده | 50 |

---

## 🏆 درجات نظامی

- **سرباز** - 0 امتیاز
- **گروهبان** - 50 امتیاز
- **ستوان** - 100 امتیاز
- **سرگرد** - 200 امتیاز
- **فرمانده** - 500 امتیاز
- **فرمانده کل** - 1000 امتیاز

---

## 🔒 امنیت

### ویژگی‌های امنیتی

✅ **حذف خودکار متادیتا**
- GPS، EXIF، زمان ساخت حذف می‌شود
- فایل‌های اصلی پاک می‌شوند
- برای امنیت کاربران ضروری است

✅ **عدم ذخیره‌سازی دائمی**
- فایل‌ها موقتی ذخیره می‌شوند
- پس از پردازش حذف می‌شوند

✅ **Spintax برای جلوگیری از شناسایی**
- توییت‌ها تصادفی هستند
- از تشخیص اسپم جلوگیری می‌کند

---

## 📞 پشتیبانی

در صورت مشکل:
1. فایل `README.md` را مطالعه کنید
2. `setup_check.py` را اجرا کنید
3. لاگ‌های ربات را بررسی کنید

---

## 💪 برای آزادی ایران!

**انقلاب ملی ۱۴۰۴**

با هم برای ایران آزاد

🦁☀️🦁☀️🦁☀️

---

## نکات مهم

1. **توکن ربات را هرگز به اشتراک نگذارید**
2. **ffmpeg برای امنیت ویدیوها ضروری است**
3. **Mini App باید روی HTTPS باشد**
4. **ربات را 24/7 روشن نگه دارید**
5. **پشتیبان از دیتابیس بگیرید**

---

موفق باشید! 💪
